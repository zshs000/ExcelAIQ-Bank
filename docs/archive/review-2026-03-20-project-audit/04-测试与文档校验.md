# 测试与文档校验

## 1. 本次实际执行的测试

### 已通过

- `mvn -q -pl eaqb-excel-parser -am -Dtest=ExcelFileServiceImplTest test`
- `mvn -q -pl eaqb-question-bank -am -Dtest=QuestionServiceImplTest test`
- `mvn -q -pl eaqb-user -am -Dtest=UserServiceImplTest test`
- `mvn -q -pl eaqb-auth -am '-Dtest=AuthServiceImplTest,VerificationCodeLoginStrategyTest,PasswordLoginStrategyTest,UserLoginReqVOValidationTest' '-Dsurefire.failIfNoSpecifiedTests=false' test`

### 过程说明

- `auth` 这组测试首次在沙箱内运行失败，原因是 Maven 需要写本地仓库缓存。
- 切换为授权外沙箱执行后，测试通过。

## 2. 测试覆盖评价

### 覆盖较好的部分

- `ExcelFileServiceImplTest`
  - 上传成功
  - 模板校验失败
  - 错误明细查询
  - 解析成功
  - 文件不存在 / 越权
  - 下游导入失败
  - 重复解析抢占失败
- `QuestionServiceImplTest`
  - 批量导入
  - 创建、分页、删除、编辑
  - 发送到 MQ
  - 状态变化并发收口
  - 审核链路
- `UserServiceImplTest`
  - 已存在用户复用
  - 并发注册唯一约束兜底
  - 默认密码编码落库

### 覆盖偏薄的部分

- `AuthServiceImplTest` 目前只验证了“非法登录类型”
- `VerificationCodeServiceImpl` 没看到对应单测
- `gateway` 的 Sa-Token 鉴权、角色校验、`TrustedInternalCallGatewayFilter` 没看到自动化测试
- `TrustedInternalRequestFilter` 这类边界组件已有基础单测，但网关侧边界测试仍偏少

## 3. 文档与代码的一致性

### 基本一致

- `Excel解析与题目处理链路梳理.md`
  - 对当前 Excel 导入和 AI 审核主链路的描述基本与代码一致
- `eaqb-question-bank/AI生成与校验契约_v1.md`
  - 与当前 `QuestionMessage` / `AIProcessResultMessage` / `AIProcessResultConsumer` 基本一致

### 已过期或需要谨慎使用

- `README.md`
  - 仍把 Redis 描述为“缓存 AI 处理结果”
  - 当前代码实际上已经是消费者直接回写数据库
- 其他历史整改文档
  - 可以帮助理解演进路径
  - 但不能替代代码事实

## 4. 建议的后续文档动作

1. 更新根 `README.md`
2. 保留 `AI生成与校验契约_v1.md` 作为事实文档
3. 把本目录的审查文档纳入 `docs` 索引
4. 后续如继续改造安全边界或密码策略，优先更新本目录和 README
