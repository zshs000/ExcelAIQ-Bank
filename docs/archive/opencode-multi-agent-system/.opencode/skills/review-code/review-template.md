# 代码审查报告

被审任务：`03-plan.md 中的 Task N`
被审变更：`git diff` / `git diff --staged`
审查者：cross-reviewer
当前轮次：第 1 轮

## 裁决

裁决：通过 / 通过但有建议 / 阻断
阻断问题：0
重要问题：0
次要问题：0
需要用户决定：0

裁决规则：
- **通过**：没有阻断问题和重要问题，可以进入下一阶段。
- **通过但有建议**：没有阻断问题，但存在重要或次要建议，可以进入下一阶段，由 Master 记录风险。
- **阻断**：存在阻断问题，或存在需要用户决定的问题；不得进入下一阶段。

## 总体意见

用一小段话说明：
- 代码变更是否符合任务计划和设计文档。
- 最大风险是什么。
- 测试是否足够证明行为正确。
- 是否建议进入下一阶段。

## 审查意见 第 1 轮

### 意见 1：<问题标题>

严重程度：阻断 / 重要 / 次要

问题位置：`<文件路径>:<行号或方法名>`

问题说明：
<具体说明问题是什么。>

为什么重要：
<说明不处理会带来什么后果。>

建议修改：
<说明建议怎么改。>

### 意见 2：<问题标题>

严重程度：阻断 / 重要 / 次要

问题位置：`<文件路径>:<行号或方法名>`

问题说明：
<具体说明问题是什么。>

为什么重要：
<说明不处理会带来什么后果。>

建议修改：
<说明建议怎么改。>

## 修复结果摘要 第 1 轮

> 本节由 Master 根据 worker 返回的结构化结果整理。
> Worker 只负责修代码、跑测试、返回结果，不修改代码审查报告。
> 如果 worker 返回中包含需要用户决定的问题，Master 必须先询问用户，不得直接进入再审。

### 对意见 1 的处理

处理判断：已修复 / 未修复 / 需要用户决定

处理说明：
<根据 worker 返回结果说明改了什么、未修复的原因，或为什么需要用户决定。>

需要用户决定：
<如果处理判断为“需要用户决定”，在这里写清楚要用户决定什么，并给出可选项。否则写“无”。>

改动文件：
- `<文件路径>`

验证结果：
<运行了哪些测试或为什么未运行。>

### 对意见 2 的处理

处理判断：已修复 / 未修复 / 需要用户决定

处理说明：
<根据 worker 返回结果说明改了什么、未修复的原因，或为什么需要用户决定。>

需要用户决定：
<如果处理判断为“需要用户决定”，在这里写清楚要用户决定什么，并给出可选项。否则写“无”。>

改动文件：
- `<文件路径>`

验证结果：
<运行了哪些测试或为什么未运行。>

## 再审意见 第 2 轮

> 本节由上一轮 cross-reviewer 填写。再审必须优先复用原 cross-reviewer task_id。
> 再审者必须先阅读上一轮审查意见、修复结果摘要和最新 diff，不得重复提出已经修复或解释成立的问题。

裁决：通过 / 通过但有建议 / 阻断
阻断问题：0
重要问题：0
次要问题：0
需要用户决定：0

### 对意见 1 的再审

再审判断：已解决 / 接受修复说明 / 仍未解决 / 等待用户决定

说明：
<说明为什么这样判断。>

### 对意见 2 的再审

再审判断：已解决 / 接受修复说明 / 仍未解决 / 等待用户决定

说明：
<说明为什么这样判断。>

### 新增意见 3：<问题标题>

严重程度：阻断 / 重要 / 次要

问题位置：`<文件路径>:<行号或方法名>`

问题说明：
<仅在再审发现新问题时填写。>

为什么重要：
<说明不处理会带来什么后果。>

建议修改：
<说明建议怎么改。>

## 各维度评估

| 维度 | 评估 | 备注 |
|------|------|------|
| 正确性 | 通过 / 有风险 / 不通过 | ... |
| 设计一致性 | 通过 / 有风险 / 不通过 | ... |
| 风格规范 | 通过 / 有风险 / 不通过 | ... |
| 测试 | 通过 / 有风险 / 不通过 | ... |
| 安全 | 通过 / 有风险 / 不通过 | ... |
| 性能 | 通过 / 有风险 / 不通过 | ... |
