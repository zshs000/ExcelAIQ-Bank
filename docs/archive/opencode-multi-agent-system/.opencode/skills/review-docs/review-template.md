# 文档审查报告

被审文档：`<路径>`
上游文档：`<路径>`
审查类型：合规审查 / 批判审查
当前轮次：第 1 轮

## 裁决

裁决：通过 / 通过但有建议 / 阻断
阻断问题：0
重要问题：0
次要问题：0
需要用户决定：0

裁决规则：
- **通过**：没有阻断问题和重要问题，可以进入下一阶段。
- **通过但有建议**：没有阻断问题，但存在重要或次要建议，可以进入下一阶段，由 Master 记录风险。
- **阻断**：存在阻断问题，或存在需要用户决定的问题；不得进入下一阶段。

## 总体意见

用一小段话说明：
- 被审文档整体是否回应了上游文档。
- 最大风险或主要优点是什么。
- 是否建议进入下一阶段。

## 审查意见 第 1 轮

### 意见 1：<问题标题>

严重程度：阻断 / 重要 / 次要

问题位置：`<被审文档路径>` 的 `<章节/行号/段落>`

问题说明：
<具体说明问题是什么。>

为什么重要：
<说明不处理会带来什么后果。>

建议修改：
<说明建议怎么改。>

### 意见 2：<问题标题>

严重程度：阻断 / 重要 / 次要

问题位置：`<被审文档路径>` 的 `<章节/行号/段落>`

问题说明：
<具体说明问题是什么。>

为什么重要：
<说明不处理会带来什么后果。>

建议修改：
<说明建议怎么改。>

## 写者回复 第 1 轮

> 本节由被审文档的写者填写。写者只能在本节追加回复，不得修改上面的审查意见原文。
> 如果任何意见的处理判断是“需要用户决定”，写者必须在返回给 Master 的总结中单独列出“需要用户决定”的问题；Master 必须先询问用户，不得直接进入再审。

### 对意见 1 的回复

处理判断：接受 / 不接受 / 需要用户决定

回复说明：
<说明为什么接受、不接受，或为什么需要用户决定。>

需要用户决定：
<如果处理判断为“需要用户决定”，在这里写清楚要用户决定什么，并给出可选项。否则写“无”。>

实际处理：
待用户确认后修改 / 已修改 `<文档路径>` 的 `<章节>` / 暂不修改

### 对意见 2 的回复

处理判断：接受 / 不接受 / 需要用户决定

回复说明：
<说明为什么接受、不接受，或为什么需要用户决定。>

需要用户决定：
<如果处理判断为“需要用户决定”，在这里写清楚要用户决定什么，并给出可选项。否则写“无”。>

实际处理：
待用户确认后修改 / 已修改 `<文档路径>` 的 `<章节>` / 暂不修改

## 再审意见 第 2 轮

> 本节由上一轮审查者填写。再审必须优先复用原审查者 task_id。
> 再审者必须先阅读上一轮审查意见和写者回复，不得重复提出已经解释成立的问题。

裁决：通过 / 通过但有建议 / 阻断
阻断问题：0
重要问题：0
次要问题：0
需要用户决定：0

### 对意见 1 的再审

再审判断：已解决 / 接受写者解释 / 仍未解决 / 等待用户决定

说明：
<说明为什么这样判断。>

### 对意见 2 的再审

再审判断：已解决 / 接受写者解释 / 仍未解决 / 等待用户决定

说明：
<说明为什么这样判断。>

### 新增意见 3：<问题标题>

严重程度：阻断 / 重要 / 次要

问题位置：`<被审文档路径>` 的 `<章节/行号/段落>`

问题说明：
<仅在再审发现新问题时填写。>

为什么重要：
<说明不处理会带来什么后果。>

建议修改：
<说明建议怎么改。>
