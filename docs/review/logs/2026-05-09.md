# 2026-05-09 代码审查日志

## 🔴 高优先级

### 1. ✅ 已解决 ~~`eaqb-common` 主代码编译失败~~

**文件**: `eaqb-framework/eaqb-common/src/main/java/com/zhoushuo/framework/commono/util/JsonUtils.java`

```java
import lombok.extern.slf4j.Slf4j;

@Slf4j
public class JsonUtils {
    public static void init(ObjectMapper objectMapper) {
        log.debug("初始化 JsonUtils 中的 ObjectMapper");
        OBJECT_MAPPER = objectMapper;
    }
}
```

`mvn -q -DskipTests compile` 在 `eaqb-common` 阶段失败:

```text
程序包org.slf4j不存在
```

~~根因是 `JsonUtils` 新增了 `@Slf4j`,Lombok 会生成 `org.slf4j.Logger` 字段,但 `eaqb-common/pom.xml` 没有声明 `slf4j-api`,该模块也不能依赖下游 Spring Boot starter 兜底。当前全量编译无法通过,应优先修复。~~

已解决:`eaqb-common/pom.xml` 已补充 `slf4j-api` 依赖,`mvn -q -pl eaqb-framework/eaqb-common -DskipTests compile` 验证通过。

### 2. ~~Nacos / 数据库密码被提交到版本化配置~~ [搁置]

**文件**: `eaqb-distributed-id-generator/eaqb-distributed-id-generator-biz/src/main/resources/config/bootstrap.yml`

```yaml
username: nacos
password: Zs20060224
```

**文件**: `eaqb-distributed-id-generator/eaqb-distributed-id-generator-biz/src/main/resources/leaf.properties`

```properties
leaf.jdbc.username=root
leaf.jdbc.password=Zs20060224
```

~~这两个文件是版本化资源,当前改动把 Nacos 和 MySQL 凭据写死进仓库。即使是本地开发密码,也会进入提交历史、日志和协作环境。建议改成环境变量 / Nacos 外部配置 / 本地 ignored 配置,并轮换已经暴露的密码。~~

**搁置原因**:当前处于纯本地开发阶段,数据库和密码均为本地环境。IP 地址未泄露(Nacos 用 `${SERVER_IP}` 引用,MySQL 为 `127.0.0.1`)。待项目进入多人协作或部署到服务器时再处理。匹配 K5。

### 3. ✅ 已解决 ~~题库管理员接口的网关鉴权路径匹配错误~~

**文件**: `eaqb-gateway/src/main/java/com/zhoushuo/eaqb/gateway/auth/SaTokenConfigure.java`

```java
SaRouter.match("/question/admin/**", r -> StpUtil.checkRole("admin"));
```

**文件**: `eaqb-gateway/src/main/resources/application.yml`

```yaml
- id: question-bank
  predicates:
    - Path=/question-bank/**
  filters:
    - StripPrefix=1
```

**文件**: `eaqb-question-bank/eaqb-question-bank-biz/src/main/java/com/zhoushuo/eaqb/question/bank/biz/controller/QuestionAdminController.java`

```java
@RequestMapping("/question/admin")
```

~~Sa-Token 在网关层按外部路径执行,题库管理员接口的外部路径是 `/question-bank/question/admin/**`,但当前只匹配 `/question/admin/**`。因此访问 `/question-bank/question/admin/outbox/failed` 时会通过登录校验,但不会触发 admin 角色校验,普通登录用户可能访问失败 outbox 列表和人工重试接口。~~

已修复:鉴权路径改为 `/question-bank/question/admin/**` 和 `/user/user/admin/**`(匹配外部完整路径)。

## 🟡 中优先级

### 1. ✅ 已解决 ~~两个 `PasswordEncoderConfig` 的 `main` 方法包含中文弯引号~~

**文件**: `eaqb-auth/src/main/java/com/zhoushuo/eaqb/auth/config/PasswordEncoderConfig.java`

```java
log.debug("BCrypt encoded result: {}", encoder.encode("qwe123"));
```

**文件**: `eaqb-user/eaqb.user.biz/src/main/java/com/zhoushuo/eaqb/user/biz/config/PasswordEncoderConfig.java`

```java
log.debug("BCrypt encoded result: {}", encoder.encode("qwe123"));
```

~~这不是合法 Java 字符串字面量。当前全量编译先停在 `eaqb-common`,修复 common 后这两个模块大概率会继续编译失败。建议删除这两个 `main` 方法,或把调试代码改成测试用例。~~

已修复:删除两个 `main` 方法(调试代码,无实际用途)。

### 2. ~~受信内部调用密钥有默认值,且未见实际配置~~ [搁置]

**文件**: `eaqb-gateway/src/main/java/com/zhoushuo/eaqb/gateway/filter/TrustedInternalCallGatewayFilter.java`

```java
@Value("${internal.auth.secret:change-me-in-prod}")
private String internalAuthSecret;
```

**文件**: `eaqb-framework/eaqb-spring-boot-starter-biz-context/src/main/java/com/zhoushuo/framework/biz/context/config/ContextAutoConfiguration.java`

```java
@Value("${internal.auth.secret:change-me-in-prod}") String internalAuthSecret
```

~~`rg` 未发现配置文件里设置了 `internal.auth.secret`,网关和业务服务会共同落到默认值 `change-me-in-prod`。这会让内部调用签名机制在默认部署下等同于公开默认密钥。建议启动时强制要求显式配置,或者至少生产环境禁止默认值启动。~~

**搁置原因**:当前本地开发阶段,配置走 Nacos,本地可能未配置此项。部署到生产环境时需在 Nacos 中显式配置。匹配 K6。

## 🟢 低优先级

### 1. ✅ 已解决 ~~调试 HTML 被放入主资源静态目录~~

**文件**: `eaqb-question-bank/eaqb-question-bank-biz/src/main/resources/static/question-debug.html`

```html
<p>模拟真实页面：查询、分页、创建、编辑、删除、批量发送队列（统一按 AI 重新生成答案）。默认请求头 userId 为 10005。</p>
```

~~该页面会随 question-bank 服务打包发布。虽然业务服务的 `TrustedInternalRequestFilter` 会拒绝未签名直连请求，但调试页面仍暴露了内部接口路径、默认用户 ID、操作语义和测试入口，容易造成误用。建议移到 `docs/`、本地调试目录或测试资源，避免进入运行时 artifact。~~

已修复：移动到 `docs/archive/question-debug.html`。

## 已知上下文匹配

- `QuestionOutboxDispatchScheduler` 缺分布式锁匹配 K1,未作为新问题重复报告。是否仍维持"当前单实例部署,暂不处理"的判断?
- `QuestionBankControllr` 拼写问题匹配 K4,未作为新问题重复报告。是否仍维持"单开分支统一修正"的判断?
- `GlobalExceptionHandler` 重复定义匹配 W8,未报告。

## 验证

```bash
mvn -q -DskipTests compile
```

结果:`eaqb-common` 单模块编译已通过。全量编译仍失败,但失败点已转移到 `eaqb-auth/src/main/java/com/zhoushuo/eaqb/auth/config/PasswordEncoderConfig.java` 的中文弯引号问题,对应本日志中优先级第 1 条。

## 汇总表

| 状态 | 数量 |
|------|------|
| 🔴🟡🟢 待处理 | 0 |
| ✅ 已解决 | 4 |
| ⏸️ 搁置 | 2 |
| ⚠️❌ 降级/误判 | 0 |
